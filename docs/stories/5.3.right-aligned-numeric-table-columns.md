# Story 5.3: Right-Aligned Table Columns for Numbers

**Status:** Done
**Epic:** Code Quality, Shared Helpers & Table Alignment Refactor
**Created:** 2026-05-23
**Last Updated:** 2026-05-23

## Story

**As a** Warehouse Operator,
**I want** all tables showing numbers (such as quantities, unit prices, total costs, and safety stock thresholds) to align both header titles and column values to the right, and display currency symbols/units in the headers,
**so that** data is easily scannable and numbers align cleanly on decimal points.

## Acceptance Criteria

1. Given table components displaying numerical fields (current stock, safety stock, price, costs), When rendered, Then both the table headers and body cells of those columns must be styled with `text-align: right`.
2. Given numeric columns in tables, When displaying values, Then the dynamic cells must render only the formatted numbers, and the unit indicators (e.g., UOM like `kg`/`liters` or currency like `$`) must be positioned inside the column header (e.g. `Current Stock (UOM)` or `Unit Price ($)`).
3. Given the dynamic `GenericTable.tsx` component, When receiving a column configuration, Then it must resolve text-alignment cleanly based on TanStack Table column definitions metadata (e.g., `meta: { align: 'right' }`).

## Tasks

- [x] Task 1: Extend `GenericTable.tsx` with text-alignment support (AC: 3)
  - Read `column.columnDef.meta?.align` in `GenericTable.tsx`'s headers (`th`) and rows (`td`) components.
  - Dynamically inject alignment styles (`textAlign: meta.align || 'left'`).
- [x] Task 2: Refactor Material Catalog Table (AC: 1, 2)
  - Update `src/app/catalog/page.tsx`'s React Table columns.
  - Configure `currentStock` and `safetyStock` headers to show units: `"Current Stock (Unit)"` and `"Safety Stock (Unit)"`.
  - Refactor cells to output only numbers.
  - Configure `meta: { align: 'right' }` on both columns.
- [x] Task 3: Refactor Transaction History Tables (AC: 1, 2)
  - Update `src/app/history/page.tsx`'s Import & Export columns.
  - Configure `quantity` (with UOM inside header), `unitPrice` (with `($)` inside header), and `totalCost` columns.
  - Remove inline styling overrides and set `meta: { align: 'right' }` on all numerical/currency columns.

## Dev Notes

### Relevant Files

- `src/components/GenericTable.tsx`
- `src/app/catalog/page.tsx`
- `src/app/history/page.tsx`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-23 | 1.0 | Initial story definition and completion | @planner |
