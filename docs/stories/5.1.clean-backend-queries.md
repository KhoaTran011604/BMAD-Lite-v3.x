# Story 5.1: Clean BE & Database Queries (0% N+1 Queries)

**Status:** Done
**Epic:** Code Quality, Shared Helpers & Table Alignment Refactor
**Created:** 2026-05-23
**Last Updated:** 2026-05-23

## Story

**As a** Farm Developer,
**I want** to write highly readable, short, and performant database queries without querying inside loops (N+1 queries),
**so that** AgriKeep remains fast, database overhead is minimized, and connection pooling is optimized.

## Acceptance Criteria

1. Given database read operations, When fetching related data, Then the application must use Mongoose `.populate()`, MongoDB aggregation pipelines (`$lookup`), or bulk queries via `$in` rather than executing queries inside loops (such as `.map()` or `for` loops).
2. Given any backend schema, When writing data access queries, Then they must be short, structured, readable, and properly utilize Mongoose indexes for optimal performance.

## Tasks

- [x] Task 1: Audit entire codebase for potential N+1 database queries (AC: 1)
  - Audit `src/app/api/materials/route.ts`, `src/app/api/materials/[id]/route.ts`, `src/app/api/imports/route.ts`, and `src/app/api/exports/route.ts`.
  - Confirm all referenced data fetches utilize Mongoose `.populate()` or bulk fetches.
- [x] Task 2: Update Coding Standards documentation (AC: 1, 2)
  - Formalize **Rule 9** in `docs/architecture/coding-standards.md` explicitly banning N+1 queries.
  - Formulate documentation explaining how database queries should be designed to be short, readable, and efficient.

## Dev Notes

### Relevant Files

- `docs/architecture/coding-standards.md`
- `src/app/api/imports/route.ts`
- `src/app/api/exports/route.ts`

### Coding Standards

- Complete Type Safety: No `any` keyword.
- Absolute Ban on N+1 Queries: 0% queries inside loops. Use `.populate()`, bulk `$in`, or aggregations.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-23 | 1.0 | Initial story definition and completion | @planner |
