# Story 6.2: Replace Simulated Authorization Header with Secure Cookies

**Status:** Done  
**Epic:** User Authentication & Role-Based Access Control (RBAC)  
**Created:** 2026-05-23  
**Last Updated:** 2026-05-23  

## Story
**As a** Farm Developer,  
**I want** existing material and transaction logging API endpoints to validate user sessions using the secure cookie instead of unauthenticated simulated headers,  
**so that** AgriKeep is protected against unauthorized role-spoofing.

---

## Acceptance Criteria
1. **Given** any write or sensitive API endpoints (`POST /api/materials`, `POST /api/imports`, `POST /api/exports`), **When** invoked, **Then** the endpoint must extract and decrypt the secure session cookie to retrieve the authenticated user profile.
2. **Given** a user role of `Worker`, **When** attempting a write operation (e.g. creating a material or logging a transaction), **Then** the server must reject the operation with a `403 Forbidden` error.
3. **Given** a request without an active session cookie, **When** attempting any write operation, **Then** the server must reject the request with a `401 Unauthorized` error.

---

## Tasks
- [x] **Task 1: Build Server Session Guard helper (`src/lib/auth-guard.ts`)**
  - Create a helper function `getServerSession(req: NextRequest)` that parses cookies, validates the signature, and returns the authenticated user payload.
- [x] **Task 2: Refactor API endpoints to use Session Guard**
  - Modify `POST /api/materials` to retrieve user session via the helper and reject unauthorized access.
  - Modify `POST /api/imports` to secure importing transactions.
  - Modify `POST /api/exports` to secure exporting transactions.
- [x] **Task 3: Refactor integration test suite simulated headers**
  - Modify existing tests under `/tests` folder to sign a mock session cookie rather than adding simulated `x-user-role` headers to avoid breaking the CI/CD pipeline.

---

## Dev Notes
### Relevant Files
- `src/lib/auth-guard.ts`
- `src/app/api/materials/route.ts`
- `src/app/api/imports/route.ts`
- `src/app/api/exports/route.ts`
- `tests/integration/`

---

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-23 | 1.0 | Story definition and tasks created | @planner |
| 2026-05-23 | 1.1 | Implemented cookie session guard for write APIs, removed simulated role headers, and updated integration tests | @executor |
