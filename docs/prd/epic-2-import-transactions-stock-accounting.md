# Epic 2: Import Transactions & Stock Accounting

> [SCANNED — needs review] Auto-generated by BMAD-Lite Scanner. Review and refine with `/bmad-plan prd`.

**Goal:** Provide warehouse operators with a transaction registry to log incoming agricultural supplies, atomically updating physical stock-on-hand and storing expiration schedules.

## Story 2.1: Log Import Transaction API & DB Update
**As a** Warehouse Operator,
**I want** an API endpoint to record incoming shipments,
**so that** inventory balances are incremented and shelf-life tracking is established.

**Acceptance Criteria:**
1. Given a POST request to `/api/imports`, When payload contains valid transaction attributes (date, supplierName, materialId, quantity, unitPrice, batchCode, expirationDate), Then it must write an import log entry and increment the material's `currentStock` level in the database atomically.
2. Given an import payload, When `quantity` or `unitPrice` is zero/negative, Then it must return a `400 Bad Request` validation error via Zod schemas.
3. Given a POST request to `/api/imports`, When simulated headers check `x-user-role`, Then it must reject requests where role is not `Manager` or `FarmManager`.

## Story 2.2: Import Transaction Form UI
**As a** Warehouse Operator,
**I want** a smooth form layout with field suggestions and date-pickers,
**so that** I can log new deliveries rapidly and avoid typing errors.

**Acceptance Criteria:**
1. Given the Import Form (QuickActionDrawer or modal), When selecting a material, Then it must provide a searchable dropdown listing all catalog items.
2. Given chemical/perishable materials, When logging an import, Then the Expiration Date and Batch Code fields must be available for entry.

## Story 2.3: Import History Log UI
**As a** Farm Manager,
**I want** a transaction feed showing recent incoming shipments,
**so that** I can review suppliers, pricing trends, and batch receipts.

**Acceptance Criteria:**
1. Given the Transaction History page (`/history`), When the user loads the page, Then it must fetch `/api/imports` sorted by date descending.
2. Given the Import history list, When rendered, Then it must be filterable by search term matching material name or supplier name.
