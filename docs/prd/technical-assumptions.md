# Technical Assumptions

> [SCANNED — needs review] Auto-generated by BMAD-Lite Scanner. Review and refine with `/bmad-plan prd`.

## Repository Structure
- **Type:** Single repository (monorepo / unified layout) [SCANNED]
- **Package Manager:** `npm` [SCANNED]
- **Rationale:** Single codebase structure for frontend components, API endpoints, schemas, and configurations allows streamlined deployments and extremely fast iterations.

## Architecture
- **Pattern:** Hybrid App Router (Layered with `/app` routing, `/models` data layer, and custom `/hooks` query layer) [INFERRED]
- **Framework:** Next.js App Router (14.2.3) [SCANNED]
- **Database:** MongoDB via Mongoose (8.3.4) [SCANNED]
- **State Management:** TanStack React Query (5.35) [SCANNED]
- **Form Management:** React Hook Form (7.51) with Zod validation [SCANNED]

## Testing
- **Framework:** Vitest (1.6.0) [SCANNED]
- **Rationale:** Essential to enforce 100% correct inventory balance calculations, transactional logging constraints, and negative stock validations under unit tests.

## Deployment
- **Platform:** Vercel or standard Node.js server container [SCANNED]

## Additional Technical Assumptions and Requests
- **Styling:** Custom Vanilla CSS for modular styling, enabling premium glassmorphism layouts and high-performance transitions without Tailwind dependencies.
- **Transactional safety:** Uses Mongoose session transactions where replica sets are present, with graceful atomic fallback in single-node environments.
