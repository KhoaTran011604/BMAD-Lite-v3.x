{
  "permissions": {
    "allow": [
      "Bash(npx tsc --noEmit)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[Doc Sync Reminder] If this change creates new CODE files -> MUST update source-tree.json (skip assets/non-code). If affects data models, API endpoints, dependencies -> update corresponding docs/architecture/ files.'"
          }
        ]
      }
    ]
  }
}